// Optional CPU microbenchmark against the pre-cache solver; requires baseline git history.
const fs=require('fs'),path=require('path'),{execFileSync}=require('child_process'),{performance}=require('perf_hooks');
function load(source){const s=source.split('<script>')[1].split('// ---------- WGSL ----------')[0];return new Function(s+';return {Engine,makeParams}')();}
const results=[];for(const [name,file]of[['previous',execFileSync('git',['show','5b193b58cd84acda32991db72969e8cac1b8ff6d:slurry/index.html'],{cwd:path.join(__dirname,'..'),encoding:'utf8'})],['optimized',fs.readFileSync(path.join(__dirname,'../slurry/index.html'),'utf8')]]){const {Engine,makeParams}=load(file),e=new Engine(40,40,500,makeParams({gy:0,jitter:0}));for(let y=10;y<20;y+=.5)for(let x=10;x<20;x+=.5)e.add(x,y,22);e.sort();e.wakePass();e.density();e.bondPass();const times=[];
for(let run=0;run<11;run++){for(let i=0;i<e.n;i++){e.nx[i]=e.px[i]+.2*Math.sin(e.py[i]*3);e.ny[i]=e.py[i]+.1*Math.cos(e.px[i]*3);}const t=performance.now();e.steelProject();if(run>2)times.push(performance.now()-t);}times.sort((a,b)=>a-b);results.push({name,ms:times[times.length>>1],x:Array.from(e.nx.slice(0,e.n)),y:Array.from(e.ny.slice(0,e.n))});}
const [a,b]=results;const error=Math.max(...a.x.map((v,i)=>Math.abs(v-b.x[i])),...a.y.map((v,i)=>Math.abs(v-b.y[i])));console.log(JSON.stringify({particles:400,iterations:128,old_ms:a.ms,new_ms:b.ms,speedup:a.ms/b.ms,max_coordinate_error:error}));if(error!==0)throw Error('solver changed');
